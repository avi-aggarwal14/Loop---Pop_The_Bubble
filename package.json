{
  "name": "synapse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Synapse — AI growth partner. Engine: ingest analytics, generate a weekly Growth Brief, remember everything via mubit.",
  "scripts": {
    "generate-brief": "tsx scripts/generate-brief.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/dates.test.ts tests/derive.test.ts tests/shopify-oauth.test.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "dotenv": "^17.4.2",
    "openai": "^6.42.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
