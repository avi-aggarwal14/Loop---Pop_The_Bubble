# ── OpenAI (Growth Brief engine) ──────────────────────────────────
# Brief generation runs on OpenAI. Server-only. Never expose to the client.
# Must be an OpenAI *API* key from platform.openai.com (not the ChatGPT/Codex app).
OPENAI_API_KEY=
# Model the briefs are generated with. Set to whatever your credits cover.
OPENAI_MODEL=gpt-5
# Reasoning depth for reasoning models (gpt-5 / o-series): minimal | low | medium | high.
# Set to "none" to omit the param entirely (e.g. for gpt-4.1 which doesn't accept it).
OPENAI_REASONING_EFFORT=medium

# ── mubit (operational memory) ────────────────────────────────────
# Get the API key + confirm the base URL/auth from https://console.mubit.ai/app/overview
# Base URL and auth header are env-driven because the gated docs may differ from the
# public /v2/control/* shapes this client was written against.
MUBIT_API_KEY=
MUBIT_BASE_URL=https://api.mubit.ai
# How the key is sent. Common options: "bearer" (Authorization: Bearer <key>) or "x-api-key".
MUBIT_AUTH_SCHEME=bearer

# ── Supabase (Postgres, Auth, storage) ────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service-role key: server-only, used by ingestion/cron jobs. NEVER ship to the client.
SUPABASE_SERVICE_ROLE_KEY=

# ── Shopify (Phase 3 — first data integration) ────────────────────
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_SCOPES=read_orders,read_customers,read_products
SHOPIFY_APP_URL=http://localhost:3000
